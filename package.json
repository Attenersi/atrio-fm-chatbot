{
  "name": "fm-chatbot-workspace",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"cd backend && venv\\Scripts\\python.exe -m uvicorn app.main:app --reload --port 8010\" \"set \\\"NEXT_PUBLIC_API_URL=http://localhost:8010\\\" && npm --prefix frontend run dev -- -p 3010\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
